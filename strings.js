// ...new file...
// Central simple strings object. No dynamic requires, static import of JSON files.
import en from './locales/en.json';
import es from './locales/es.json';

export const STRINGS = {
  en,
  es,
};

export default STRINGS;
